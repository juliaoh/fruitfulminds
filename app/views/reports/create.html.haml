
%p{:style => "width: 100%; height: 60px; margin: 0 auto;"}
  The following is a preview
  %br
  In order to generate a PDF report, add comments at the bottom of the page

- if @warning_flag == true
  - @warnings.each do |message|
    %p= message
  %p= "If necessary, please add ambassador notes to explain the warnings."



%h2= @main_title
%h4= @intro_title
%p= @introduction
%h4= @school_intro_title
%p= @school_intro
%p= @school_intro_second
%p= @school_intro_third
%h4= @objectives_title

%table
  %tr
    %th Lesson
    %th Objective
  - @objectives.each do |item1, item2|
    %tr
      %td= item1
      %td= item2

%h4= @eval_title
%p= @eval_intro_first
%p= @eval_intro_second
%p= @eval_intro_third
%h4= image_tag @nutrition_chart
%h4= image_tag @combined_chart
%h4= @strength_weakness_title
%p= @strength_weakness_intro

%h4= @strength_intro
%table
  %tr
  - @objective_str.values.each do |strength|
    %tr
      %ul
        %li= strength

%h4= @weakness_intro
%table
  %tr
  - @objective_weak.values.each do |weakness|
    %tr
      %ul
        %li= weakness

%h4= @comp_intro
%table
  %tr
  - @objective_comp.values.each do |comp|
    %tr
      %ul
        %li= comp

%p
%p= @summary

%h5= @behavior_title
%p= @behavior_intro
%h4= image_tag @efficacy_chart

%p= @improvement_intro

%h5= @increase_header
%table
  %tr
  - @efficacy_str.values.each do |q_msg|
    %tr
      %ul
        %li= q_msg

%h5= @decrease_header
%table
  %tr
  - @efficacy_weak.values.each do |q_msg|
    %tr
      %ul
        %li= q_msg

%h5= @comp_header
%table
  %tr
  - @efficacy_comp.values.each do |q_msg|
    %tr
      %ul
        %li= q_msg

%h5= @summary_header

%table
  %tr
  - @summary_messages.each do |msg|
    %tr
      %td= msg





%h4= @ambassadorNoteTitle
-# %p= @report.note
= render :partial => 'comment_form'
